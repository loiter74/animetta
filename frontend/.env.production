# Production environment
# VITE_API_URL is intentionally left unset for Docker deployment.
# The frontend connects to window.location.origin (same origin via nginx proxy).
# Set VITE_API_URL to an explicit URL when deploying to an external host (e.g., ngrok).
# VITE_API_URL=https://your-domain.example.com
