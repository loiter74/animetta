## No Spec Changes

This change is a pure bug fix — no new capabilities introduced, no existing behavior requirements modified.

- `AudioAnalyzer.compute_volume_envelope` API signature unchanged
- No new configuration or user-facing behavior
- No spec-level requirement changes needed
