## No Spec-Level Changes

This change is a pure implementation bugfix with no requirement-level changes.
No new capabilities, no modified requirements — all four bugs are mismatches
between frontend and backend implementation that violate the existing contract.
