fastapi>=0.100.0
uvicorn>=0.23.0
sanic==22.12.0
# Socket.IO - pin compatible versions (tested with Sanic 23.3.0)
python-socketio==5.10.0
python-engineio==4.8.0
websockets>=11.0
pydantic>=2.0.0
pyyaml>=6.0
loguru>=0.7.0
numpy>=1.24.0
torch>=2.0.0
torchaudio>=2.0.0
edge-tts>=6.1.0

# Kokoro TTS (open-weight multilingual TTS, 82M params)
# For Chinese support: pip install misaki[zh]
# Models auto-download from HuggingFace on first use (~300MB)
kokoro>=0.9.4

python-dotenv>=1.0.0
pydub>=0.25.0
# Python 3.13+ compat: audioop was removed, audioop-lts backports it
audioop-lts>=0.2.2

# Open source free ASR
faster-whisper>=1.0.0
funasr>=1.0.0
modelscope>=1.0.0
# Audio processing optimization (optional, for better resampling)
# librosa>=0.10.0
# resampy>=0.4.0

# Local LLM support (for LocalLoraLLM)
transformers>=4.36.0
peft>=0.7.0
accelerate>=0.25.0

# Memory and embeddings (for vector search)
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
chromadb>=0.4.0

# GLM API (for GLM LLM and ASR)
zhipuai>=2.0.0

# VAD (Voice Activity Detection)
silero-vad>=5.0.0

# LangGraph - Agent 状态图框架
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# MCP (Model Context Protocol) - 工具协议
mcp>=0.1.0

# 可观测性 (Observability)
langfuse>=4.0.0
opentelemetry-api>=1.30.0
opentelemetry-sdk>=1.30.0
opentelemetry-exporter-otlp-proto-grpc>=1.30.0

# Chinese text conversion (Traditional → Simplified)
opencc-python-reimplemented>=0.1.7

# Stats storage (Pipeline stats dashboard)
aiosqlite>=0.20.0

# HTTP client (Notifier webhook delivery)
httpx>=0.27.0

# JWT - 兼容 zhipuai 和 mcp 的版本
# 注意：zhipuai 声称需要 <2.9.0，但实际测试 2.10.x 可正常工作
pyjwt>=2.10.0

# Bilibili 直播弹幕接收
bilibili-api-python>=17.0.0

# Optional: LLM evaluation framework (scripts/eval_llm.py)
# sentence-transformers>=3.0

# Optional: Redis checkpoint support (multi-instance session sharing)
redis[hiredis]>=5.0
